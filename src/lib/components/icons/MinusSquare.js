import React from 'react';
import RemoveIcon from '@material-ui/icons/Remove';
export const MinusSquare = props => {
  return (
    <RemoveIcon fontSize="inherit" style={{ width: 16, height: 16 }} {...props} />
  );
}